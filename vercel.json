{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "cd apps/web && pnpm build",
  "devCommand": "cd apps/web && pnpm dev",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "bash -c 'echo \"Branch: $VERCEL_GIT_COMMIT_REF\"; if [[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]]; then echo \"Skipping build for non-main branch\"; exit 0; else echo \"Building main branch\"; exit 1; fi'",
  "framework": "nextjs",
  "functions": {
    "apps/web/src/app/**": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"]
}
